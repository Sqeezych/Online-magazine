import {
	authorize,
	logout,
	register,
	fetchCategories,
	fetchProducts,
	createProduct,
} from './operations';

export const server = {
	authorize,
	logout,
	register,
	fetchCategories,
	fetchProducts,
	createProduct,
};
